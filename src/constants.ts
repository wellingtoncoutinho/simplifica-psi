import { Patient, Session, Transaction } from './types';

export const MOCK_PATIENTS: Patient[] = [];

export const MOCK_SESSIONS: Session[] = [];

export const MOCK_TRANSACTIONS: Transaction[] = [];
